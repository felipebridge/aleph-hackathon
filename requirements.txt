# --- Core data / business logic ---
pandas>=2.2,<3.0
python-dateutil>=2.9,<3.0
rapidfuzz>=3.9,<4.0

# --- Terminal reporting UI ---
rich>=13.7,<14.0

# --- Local / offline OCR ---
# Primary engine: Tether's local inference SDK. The client spawns/attaches to a
# local worker process over RPC -- no network calls, models run entirely
# on-device. If the worker binary isn't available in a given environment, the
# agent transparently falls back to the Tesseract engine below, so the
# pipeline is never blocked on this dependency during the demo.
tetherto-qvac-sdk>=0.17,<1.0

# Offline fallback OCR engine (also 100% local — requires the `tesseract` binary on PATH).
pytesseract>=0.3.10,<0.4.0
Pillow>=10.3,<11.0
